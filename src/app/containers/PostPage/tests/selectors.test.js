// import { fromJS } from 'immutable';
// import { makeSelectPostPageDomain } from '../selectors';

// const selector = makeSelectPostPageDomain();

describe('makeSelectPostPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
